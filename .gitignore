# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Node / UI
node_modules/
ui/dist/

# OS
.DS_Store

# Local env
.env

# Derived reconciliation output; regenerate with `make reconcile` / `make score`.
# EXCEPTIONS.md is committed on purpose: it is the honest half of the result.
data/reconciliation.json
data/score.json
